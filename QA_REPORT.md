# QA report — VZHDO Dashboard v7.2

Проверено перед упаковкой:

- `node --check app.js` — успешно.
- `npm ci` — успешно.
- `npm run build` — успешно; создаётся `dist/`.
- GitHub Pages runtime paths `/VZHDO/structure.xlsx`, `/VZHDO/app.js`, `/VZHDO/assets/vzhdo-logo.png` — подготовлены в `dist/`.
- `structure.xlsx` — runtime master; организационные фильтры пересчитываются после загрузки структуры.
- e-Өтініш: Ф.И.О. видно в аналитике, фильтра по Ф.И.О. нет, `Должность` не выводится.
- Неоднозначное Ф.И.О. не назначает подразделение автоматически; возвращается `Не определено` с предупреждением.
- KPI `Срок до 7 дней` больше не конфликтует со старой вкладкой `Просроченные`.
- Ручной месяц/год документа переводит период в произвольный и не игнорируется.
- Текстовые сроки (`постоянно`, `еженедельно`, `ежемесячно` и фактические текстовые значения из файла) отображаются и фильтруются как текст.
- 3.xlsx принимает как `Подразделение`, так и `Структурное подразделение` для audit; официальное СП определяется по Ф.И.О. из `structure.xlsx`.
- Если в 3.xlsx нет `Ответственный исполнитель`, запись не отбрасывается: СП = `Не определено`, выводится предупреждение.
- Добавлены предупреждения по несовпадению исходного подразделения и structure.xlsx, неизвестным статусам, отсутствующим срокам и возможным дубликатам.
- Добавлен пересчёт сроков при смене календарной даты без перезагрузки страницы.
- Добавлен GitHub Actions workflow `.github/workflows/deploy.yml`.
- `public/structure.xlsx` актуализирован: `НОР-2 Акмола`, `НОР-10 Туркестан`.
